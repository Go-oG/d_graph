name: dart_graph
description: "一个面向 Flutter 绘制、几何计算、图算法和空间索引的工具库"
version: 1.2.5
homepage: https://github.com/Go-oG/d_graph

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  quiver: ^3.2.2
  collection: ^1.19.1
  bezier: ^1.2.0
  vector_math: ^2.2.0
  dts: ^0.1.6
  d_util: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
