name: dart_graph
description: "A geometry util library"
version: 1.2.0
homepage: https://github.com/Go-oG/d_graph

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  quiver: ^3.2.2
  collection: ^1.19.1
  bezier: ^1.2.0
  vector_math: ^2.2.0
  dts: ^0.1.1
  d_util: ^1.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
