import 'dart:ui';

import 'package:dart_graph/dart_graph.dart';

void main() {
  // List<BasicGeometry> list = [
  //   Arc(innerRadius: 0, outRadius: 10, startAngle: -45, sweepAngle: 45), //错误
  //   Circle(center: Offset.zero, radius: 10),
  //   SegmentLine(Offset(0, 0), Offset(10, 10)),
  //   RayLine(Offset(0, 0), Offset(10, 10)),
  //   Line(Offset(0, 0), Offset(10, 10)),
  //   Polygon([
  //     Offset(0, 10),
  //     Offset(10, 10),
  //     Offset(10, 0),
  //     Offset(0, 0),
  //     Offset(0, 10),
  //   ]),
  //   Triangle(a: Offset.zero, b: Offset(10, 20), c: Offset(20, 0))
  // ];
  //
  // for (var g in list) {
  //   for (var g2 in list) {
  //     int st = DateTime.now().millisecondsSinceEpoch;
  //     final res = g.isOverlap(g2);
  //     print("${g.runtimeType} overlap ${g2.runtimeType} $res 耗时:${DateTime.now().millisecondsSinceEpoch - st}");
  //   }
  // }
  //
  // print("=============分割==================");
  //
  // // for (var g in list) {
  // //   print("contains ${g.runtimeType} ${circle.contains(g)}");
  // // }
}
